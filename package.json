{
  "name": "popular-mc-server-scraper",
  "version": "1.0.0",
  "description": "Scraper to scrape top played minecraft servers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start-build": "npm run build && node build/index.js",
    "start": "node build/index.js"
  },
  "author": "imexoodeex",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "node-cron": "^3.0.2",
    "puppeteer": "^19.7.2",
    "undici": "^5.20.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.0",
    "@types/puppeteer": "^7.0.4",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "nodemonConfig": {
    "ignore": [
      "export/*"
    ]
  }
}
